const key='choiron-free-admin-list',esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let records=[];try{records=JSON.parse(localStorage.getItem(key)||'[]');if(!Array.isArray(records))records=[];}catch{}
function render(){document.querySelector('#list').innerHTML=records.map((r,i)=>`<div class="upcoming"><div><h3>${esc(r.name)}</h3><p>${esc(r.contact)}</p></div><button class="btn secondary" data-remove="${i}">기록 삭제</button></div>`).join('')||'<p>아직 기록한 관리자가 없습니다.</p>';}
function save(){try{localStorage.setItem(key,JSON.stringify(records));}catch{document.querySelector('#result').textContent='기기 저장이 제한되었습니다.';}}
document.querySelector('form').onsubmit=e=>{e.preventDefault();const r=Object.fromEntries(new FormData(e.target));records.push(r);save();render();const url=new URL('./start.html',location.href).href;document.querySelector('#result').innerHTML=`<h3>복사해서 관리자에게 보내 주세요</h3><textarea rows="7" readonly>${esc(`${r.name} ChoirON 관리자 설정 안내입니다.\n아래 링크를 열어 자신의 Google 계정으로 설정해 주세요.\n${url}\n별도 서버 구매 없이 무료 서비스 한도 안에서 사용합니다. 최초 Apps Script 배포가 한 번 필요합니다.\n설정 완료 후 단원 연결 링크를 보내 주세요.`)}</textarea><p class="muted">안내문을 만들었습니다. 이메일이나 메시지가 자동 발송되지는 않습니다.</p>`;};
document.querySelector('#list').onclick=e=>{const b=e.target.closest('[data-remove]');if(b){records.splice(Number(b.dataset.remove),1);save();render();}};render();
